# Exclude test directory (contains large VM images)
test/

# Exclude git history
.git/
.gitignore

# Exclude logs
install/logs/

# Exclude any backups
*.backup
.dotfiles.backup*

# Exclude private data
git/

# Note: .cache/ is intentionally NOT excluded
# The .cache/pacman/pkg/ directory is used to speed up Docker builds
# by including pre-downloaded packages from the host system
